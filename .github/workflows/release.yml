name: release

# Merging a pull request into `release` publishes a stable version: the
# workflow gates, tags v<package.version>, publishes to npm, and creates the
# GitHub Release. Every step is idempotent, so re-merging an already-published
# version is a safe no-op.
on:
  pull_request:
    branches: [release]
    types: [closed]

jobs:
  publish:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write # tag push + GitHub Release
      id-token: write # npm Trusted Publishing (OIDC)
    steps:
      - uses: actions/checkout@v6
        with:
          ref: release
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      # Trusted Publishing requires npm >=11.5.1. Node 22 currently bundles
      # npm 10, so pin an OIDC-capable npm before the publish step.
      - run: npm install --global npm@11.19.0
      - run: pnpm install --frozen-lockfile
      # check includes the tsdown build, which produces the lib/ files npm
      # publishes (lib/ is gitignored, so it must exist before publishing).
      - run: pnpm run check
      - run: pnpm test
      - name: resolve version
        id: version
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"
      - name: tag the release
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if git rev-parse "refs/tags/v$VERSION" >/dev/null 2>&1; then
            echo "tag v$VERSION already exists; skipping"
          else
            git tag "v$VERSION"
            git push origin "v$VERSION"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
      - name: publish to npm
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ALREADY="$(npm view "dsh-workspace-scope@$VERSION" version 2>/dev/null || true)"
          if [ -n "$ALREADY" ]; then
            echo "dsh-workspace-scope@$VERSION already published; skipping"
          else
            node scripts/publish-trusted.mjs
          fi
      - name: create GitHub release
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if gh release view "v$VERSION" >/dev/null 2>&1; then
            echo "release v$VERSION already exists; skipping"
          else
            gh release create "v$VERSION" --title "v$VERSION" --notes "See [CHANGELOG.md](CHANGELOG.md)."
          fi
        env:
          GH_TOKEN: ${{ github.token }}
